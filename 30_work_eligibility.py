# checking of person is eligible to work.
# age must be within 18-60.

age = int(input("Enter age = "))

if (age >= 18) and (age <= 60):
    print("Eligible")
else:
    print("Not eligible")
