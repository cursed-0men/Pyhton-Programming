from tkinter import *

# '*' means all
# NOTE : 
'''
window -> container to hold or contain widgets.
widgets -> GUI elements i.e buttons, textboxes, labels, images...
'''

window_name = Tk() # instanciate an instance of window 
window_name.mainloop() # displays window